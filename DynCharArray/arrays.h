#ifndef ARRAYS_H
#define ARRAYS_H

char *ArrOversize(char *arr, size_t &size);
char *StringEnter(char *p, size_t &size);
void MyStrCpy(const char *st1,const char *st2, char *st3);
size_t MyStrLen(const char *st);
bool MyStrCmp(const char *st2, const char *st1);

#endif // ARRAYS_H
